FRONTEND VERCEL:
1. Push le dossier racine (avec index.html) sur Vercel
2. Dans index.html ligne 259, remplacer API_BASE par votre URL Render
3. Deploy

BACKEND RENDER:
1. Aller sur render.com
2. New Web Service
3. Push le dossier backend/ seul
4. Build: pip install -r requirements.txt
5. Start: uvicorn main:app --host 0.0.0.0 --port $PORT
6. Deploy
7. Copier l'URL (https://xxx.onrender.com)
8. Mettre à jour dans index.html
